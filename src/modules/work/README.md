# Work domain

Owns task, project, and first-class monthly-retainer rules. Interactive
requests, accepted proposals, and background workers call its public commands;
they must delegate persistence to the activity module's transactional mutation
service rather than updating a domain table directly.
